/*

#include<iostream>
#include<vector>
using namespace std;

int main()
{
	vector<int> ivec;
	int ival;
	float sum = 0;
	while (cin >> ival)
		ivec.push_back(ival);
	
	for (int ix = 0; ix < ivec.size(); ++ix)
		sum += ivec[ix];

	float average = sum / ivec.size();
	cout << "Sum of " << ivec.size()
		<< " elements: " << sum
		<< ". Average: " << average << endl;
	getchar();
	return 0;
}

*/